<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mas.ethan.mas_myshadow">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyLoginActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MyMainActivity"
            android:label="@string/title_activity_my_main" />
        <activity android:name=".MyProductActivity" />
        <activity android:name=".PreviewActivity" />
        <activity android:name=".MyRegisterActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".SampleLoadActivity" />
        <activity android:name=".MyCameraActivity" />
        <activity android:name=".MyUploadActivity" />
        <activity android:name=".models.NewSkinToneActivity" />
        <activity android:name=".NewSkinToneActivity" />
        <activity android:name=".EditSkinToneActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".MyUpload2Activity" />
        <activity android:name=".CompareSkinActivity"></activity>
    </application>

</manifest>